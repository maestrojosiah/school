<div class="row" 
{# style="background: url({{ asset('http://127.0.0.1:8001/images/letterhead.jpg') }}); height:100%; background-position:center; background-repeat:no-repeat; background-size:cover;"#}>
{# 	{% for setting in app.user.configs %}
		{{ dump(setting) }}
	{% endfor %}
 #}	<h3>{{ app.user.configs|first.schoolName }}</h3>
	<p>{{ app.user.configs|first.schoolAddress }}<br />
	{{ app.user.configs|first.schoolTelephone }}<br />
	{{ app.user.configs|first.documentHeader|raw }}</p>

